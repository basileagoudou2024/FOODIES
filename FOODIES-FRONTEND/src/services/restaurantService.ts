/*
import axios from 'axios';

// Définissez l'URL de votre backend
const BASE_URL = 'http://localhost:5000/api/restaurants'; // Backend local

// Fonction pour récupérer la liste des restaurants
export async function fetchRestaurants() {
  try {
    const response = await axios.get(BASE_URL);
    console.log('Réponse de l\'API:', response.data); // log pour vérifier la réponse
    return response.data;
  } catch (error) {
    console.error('Erreur lors de la récupération des restaurants', error);
    throw error;
  }
}          */
